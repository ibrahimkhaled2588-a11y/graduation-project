Upload UI note (ECG):
- Upload field is the <input type="file" id="imageUpload" name="image"> in ecg.html
- JS in ecg.html updates:
  - #previewImage (preview)
  - #uploadMeta (selected filename / error)
- Optional drag/drop implemented by ecg.html JS by dropping files into the upload wrapper.

